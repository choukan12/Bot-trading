import requests
import time

# --- CONFIGURATION FINALE ---
TOKEN = "8557896385:AAHksYybxNK4e2AM-nErbGRPjHkcn4dyDd8"
CHAT_ID = "6377841951"

def send_msg(text):
    try:
        url = f"https://api.telegram.org/bot{TOKEN}/sendMessage?chat_id={CHAT_ID}&text={text}"
        requests.get(url)
    except Exception as e:
        print(f"Erreur envoi Telegram : {e}")

print("Bot de trading activé...")
send_msg("🚀 Salut Ismaël ! Ton bot est opérationnel. Je surveille maintenant le marché pour toi.")

while True:
    # Ici on ajoutera tes indicateurs RSI / Bollinger plus tard
    time.sleep(60)
